# Add any API keys / secrets here (copy this file to .env and fill in).
# Prefix with PUBLIC_ if the value is exposed to the client (browser) bundle.
# Values used by GitHub Actions come from repo secrets, not this file.
PUBLIC_LASTFM_API_KEY=
PUBLIC_LASTFM_USER=
